$(document).ready(function() {
    /*FireBase Test
    var url = "https://scorching-fire-2606.firebaseio.com";
    var fbRef = new Firebase(url);
    $("a.subbmitForm").click(function(e) {
        e.preventDefault();
        var countryVal = $("input[name=country]").val();
        var today = new Date();
        var dateVal = today;
        console.log(countryVal);
        console.log(today);
        console.log(dateVal);
        if(countryVal!==null){
            fbRef.push({
                date: dateVal,
                country: countryVal
            }, function(error) {
                if (error) {
                    alert('Data could not be saved.' + error);
                } else {
                    alert('Data saved successfully.');
                }
            });
            else{
                alert('Please fill in all input boxes before subminting your answers!')
            }
        };
    });*/
});